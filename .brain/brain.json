{
    "meta": {
        "schema_version": "1.1.0",
        "awf_version": "3.3.0"
    },
    "project": {
        "name": "RSS Reader",
        "type": "Android App",
        "status": "In Maintenance"
    },
    "tech_stack": {
        "frontend": {
            "language": "Kotlin",
            "ui": "Jetpack Compose"
        },
        "backend": {
            "local_db": "Room",
            "api": "Gemini API"
        },
        "tools": [
            "Gradle",
            "ADB",
            "Python/PySide6"
        ]
    },
    "database_schema": {
        "tables": [],
        "relationships": []
    },
    "api_endpoints": [],
    "features": [
        {
            "name": "Gemini API Error Handling",
            "status": "Active",
            "details": "Handles 429 (Quota Exceeded) with 30-hour persistent ban and 503 (Server Busy) with 5-minute cooldown per model/key."
        },
        {
            "name": "News Reading",
            "status": "Active",
            "details": "Optimized with Regex-First extraction and modular Facade architecture."
        },
        {
            "name": "Text-to-Speech",
            "status": "Active"
        },
        {
            "name": "Media Button Control",
            "status": "Fixed",
            "details": "Supports Headset/Car controls with Mutex-based debouncing"
        },
        {
            "name": "Debug Logging",
            "status": "Active"
        },
        {
            "name": "Resume Reading",
            "status": "Active",
            "details": "Can resume from last sentence after phone call interruption"
        },
        {
            "name": "Gemini API Usage Logging",
            "status": "Active",
            "details": "Detailed logs for API calls (Start, Success, Error, Fallback) with per-model tracking and Gemini-specific UI filter."
        },
        {
            "name": "Gemini Client Modularization",
            "status": "Completed",
            "details": "Refactored GeminiApiClient into modular package (com.skul9x.rssreader.data.network.gemini) with GeminiModels.kt, GeminiPrompts.kt, GeminiResponseHelper.kt. All consumer files updated."
        },
        {
            "name": "Activity Log Management",
            "status": "Active",
            "details": "Supports filtering by All, Errors, and Gemini events. Automatically cleans up logs older than 7 days."
        },
        {
            "name": "Robust JSON Parsing",
            "status": "Fixed",
            "details": "Uses Regex-based extraction to reliably parse JSON from Gemini's markdown code block responses."
        },
        {
            "name": "Sentence-level TTS Resume",
            "status": "Fixed",
            "details": "Splits text into sentences to allow precise resuming mid-article after interruptions (calls, focus loss)."
        }
    ],
    "knowledge_items": {
        "patterns": [
            {
                "name": "Quota Management",
                "description": "Use ModelQuotaManager to track availability of resources (API keys/Models) to prevent wasted calls."
            },
            {
                "name": "Service-based Architecture",
                "description": "NewsReaderService handles background playback"
            },
            {
                "name": "Fire-and-Forget for Non-Critical IO",
                "description": "Use launch() instead of withContext() for database operations that shouldn't block UI/navigation. Example: markAsRead() in skip operations."
            },
            {
                "name": "Regex-First Extraction",
                "description": "Prioritize Regex over Jsoup for known domains (Genk, VnExpress, Tuoi Tre) to reduce parsing overhead. Fallback to Jsoup if Regex fails."
            },
            {
                "name": "Facade Pattern for Legacy Refactoring",
                "description": "When refactoring large God classes, create a thin Facade that delegates to smaller components (Strategy, Utils) but keeps the original public API to avoid breaking consumers."
            },
            {
                "name": "Modular API Helpers",
                "description": "Extract logic for different API responsibilities (Models, Prompts, Response Parsing) into a specialized package to keep clients lightweight."
            },
            {
                "name": "Unified Synchronization",
                "description": "Always use a single synchronization mechanism (e.g., Mutex) for a shared state to avoid race conditions between Coroutine and Thread contexts."
            },
            {
                "name": "Batch Operation Chunking",
                "description": "Always chunk large data operations (upload/delete/write) when dealing with external API limits (e.g., Firestore 500-op limit). Chunk at 400 for a safe margin."
            },
            {
                "name": "Model-First Rotation",
                "description": "Strategy for multi-model/multi-key APIs: try all keys for the highest quality model first, then fall back to lower tier models. Ensures best results within quota limits."
            },
            {
                "name": "SHA-256 Key Hashing for Storage",
                "description": "Never store API keys or secrets in plaintext in SharedPreferences/Settings. Use truncated SHA-256 hashes as lookup keys instead."
            },
            {
                "name": "WorkManager Unique Work",
                "description": "Always use `enqueueUniqueWork` for operations that manipulate state to prevent race conditions from concurrent workers."
            },
            {
                "name": "Avoid Recursive Scheduling",
                "description": "Do not mix PeriodicWork and OneTimeWork self-chaining for the same task, as it creates uncontrolled polling loops."
            },
            {
                "name": "Single Transaction Batch SQL",
                "description": "Wrap N separate inserts in a @Transaction block to ensure the OS only performs one fsync, dramatically increasing I/O performance."
            },
            {
                "name": "Persistent Application Scope",
                "description": "Use a SupervisorJob scope tied to the Application lifecycle for critical fire-and-forget tasks (like onStop cloud flush) to prevent cancellation during activity death."
            },
            {
                "name": "Inclusive Cursor Bounds",
                "description": "When syncing by timestamp, use `>=` with a 1ms buffer to ensure no items are skipped if they were updated at the exact same millisecond as the cursor."
            },
            {
                "name": "Global Lifecycle Cleanup",
                "description": "Coordinate multi-module state reset (Singletons, DB, WorkManager) in an Application-level onSignOut method to ensure user data isolation."
            }
        ],
        "gotchas": [
            {
                "name": "Gemini Thinking Tokens (2.5+ Models)",
                "description": "Models like gemini-2.5-flash return reasoning in parts[0] by default. MUST iterate through all 'parts' and skip those with 'thought: true' to get actual content."
            },
            {
                "name": "Mocking Static Log Methods",
                "description": "Use mockkStatic(android.util.Log::class) to prevent 'Method not mocked' errors in unit tests checking logs."
            },
            {
                "name": "Media Button Blocking",
                "description": "Avoid using flags for debouncing async media events. Use Mutex.tryLock() with strictly enforced try-finally blocks."
            },
            {
                "name": "Service Lifecycle",
                "description": "startForegroundService can restart service with empty state if process was killed."
            },
            {
                "name": "Mutex Deadlock from Suspend Functions",
                "description": "CRITICAL: Inside a Mutex-protected coroutine, NEVER use withContext() or other suspend functions that might block. Use launch() (fire-and-forget) instead to prevent mutex from being held forever."
            },
            {
                "name": "AudioFocus Loss Stops TTS",
                "description": "TtsManager's AudioFocusChangeListener calls stop() on focus loss events. This can happen unexpectedly during audio stream transitions."
            },
            {
                "name": "Android API in Unit Tests",
                "description": "Classes like android.text.Html or android.util.Log cannot be mocked directly in JVM unit tests. Must use Dependency Injection with Interfaces (AppLogger, HtmlCleaner)."
            },
            {
                "name": "Short Content Filtering",
                "description": "Extractors have logic to return null if content < 20-50 chars. Ensure test data exceeds this limit."
            },
            {
                "name": "Kotlin Nested TypeAlias",
                "description": "Kotlin does not support nested type aliases to same-named classes well. Move data classes to top-level if collision occurs during refactoring."
            },
            {
                "name": "Mixed Synchronization (Mutex vs @Synchronized)",
                "description": "CRITICAL: Never mix Coroutine Mutex and JVM @Synchronized on the same shared state. They operate on different levels and don't block each other, leading to race conditions."
            },
            {
                "name": "Double-Prompting in Call Chains",
                "description": "High risk when one helper builds a prompt and passes it to another helper that also bọc (wraps) its input. Ensure raw prompts are only built once."
            },
            {
                "name": "Firestore Batch Limits",
                "description": "Batch writes in Firestore are limited to 500 documents. Exceeding this causes a crash. Always chunk input lists."
            },
            {
                "name": "Silent Failure in Sync",
                "description": "Catching exceptions and returning empty results in sync repositories prevents the coordinator from knowing a retry is needed. Always rethrow after logging."
            },
            {
                "name": "Earliest Wins Conflict Resolution",
                "description": "For 'Read Status' sync, the earliest timestamp is the source of truth (the moment the user first read the item). Logic must use `remote < local` comparison."
            },
            {
                "name": "Hard WakeLock Battery Drain",
                "description": "Holding a WakeLock with a hard timeout (e.g., 30 mins) prevents the CPU from sleeping even if task finishes. Use a dynamic monitor that polls state and releases early."
            },
            {
                "name": "Android 8+ Service Background Start",
                "description": "Calling startService() when the app is in the background on Android 8+ will crash with BackgroundServiceStartNotAllowedException. Use startForegroundService() instead."
            }
        ],
        "conventions": [
            "Use DebugLogger for runtime troubleshoot",
            "Always add DebugLog before/after mutex operations",
            "Non-blocking IO for navigation (NEXT/PREV) operations",
            "Use AppLogger interface instead of static Log calls for testability",
            "Use HtmlCleaner interface instead of static Html.fromHtml for testability",
            "Standardize Gemini helpers in .data.network.gemini package",
            "Chunk all Firestore batch operations at 400 items",
            "Hash secrets (API keys) before using them as keys in persistent storage."
        ]
    }
}